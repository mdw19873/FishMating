name: FishMating
version: '${project.version}'
main: com.mrsuffix.fishmating.FishMatingPlugin
author: mrsuffix
description: A plugin that simulates fish breeding triggered by throwing seeds into water
api-version: '1.21'
website: https://github.com/mrsuffix/fishmating

softdepend: [WorldGuard]

commands:
  fishmating:
    description: FishMating administrative commands
    usage: /fishmating <reload|status|nearby|config|grow>
    aliases: [fm]

permissions:
  fishmating.admin:
    description: Allows reloading the config and using the status/nearby/config/grow admin commands
    default: op
